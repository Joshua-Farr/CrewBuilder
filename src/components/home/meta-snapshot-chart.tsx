"use client";
import * as React from "react";
import { Area, AreaChart, CartesianGrid, ResponsiveContainer, Tooltip, XAxis, YAxis } from "recharts";
import type { TrendPoint } from "@/lib/types";
export function MetaSnapshotChart({ points }: { points: TrendPoint[] }) { const [mounted, setMounted] = React.useState(false); React.useEffect(() => setMounted(true), []); const luffy = points.filter((point) => point.leader === "Monkey D. Luffy"); if (!mounted) return <div className="h-72 w-full rounded-3xl bg-white/5" />; return <div className="h-72 w-full"><ResponsiveContainer><AreaChart data={luffy} margin={{ left: 0, right: 12, top: 10, bottom: 0 }}><defs><linearGradient id="playRate" x1="0" y1="0" x2="0" y2="1"><stop offset="5%" stopColor="#38bdf8" stopOpacity={0.55} /><stop offset="95%" stopColor="#38bdf8" stopOpacity={0} /></linearGradient></defs><CartesianGrid stroke="rgba(148,163,184,0.16)" vertical={false} /><XAxis dataKey="date" stroke="#94a3b8" tickLine={false} axisLine={false} /><YAxis stroke="#94a3b8" tickLine={false} axisLine={false} /><Tooltip contentStyle={{ background: "#08111f", border: "1px solid rgba(148,163,184,0.25)", borderRadius: 16 }} /><Area type="monotone" dataKey="playRate" stroke="#38bdf8" strokeWidth={3} fill="url(#playRate)" /></AreaChart></ResponsiveContainer></div>; }
