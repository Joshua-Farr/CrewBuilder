"use client";
import { Menu, Search, Shield } from "lucide-react";
import Link from "next/link";
import { usePathname } from "next/navigation";
import { Button } from "@/components/ui/button";
import { cn } from "@/lib/utils";
const nav = [{ href: "/decks", label: "Decks" }, { href: "/meta", label: "Meta" }, { href: "/cards", label: "Cards" }, { href: "/tournaments", label: "Tournaments" }, { href: "/builder", label: "Builder" }];
export function SiteHeader() { const pathname = usePathname(); return <header className="sticky top-0 z-40 border-b border-white/10 bg-slate-950/75 backdrop-blur-xl"><div className="mx-auto flex h-16 max-w-7xl items-center justify-between px-4 sm:px-6 lg:px-8"><Link href="/" className="flex items-center gap-3 font-black tracking-tight"><span className="flex size-10 items-center justify-center rounded-2xl bg-gradient-to-br from-sky-400 via-violet-500 to-yellow-300 text-slate-950 shadow-lg shadow-sky-500/20">GL</span><span className="hidden text-lg sm:inline">Grand Line Meta</span></Link><nav className="hidden items-center gap-1 lg:flex">{nav.map((item) => <Link key={item.href} href={item.href} className={cn("rounded-full px-4 py-2 text-sm font-semibold text-muted-foreground transition hover:bg-white/10 hover:text-foreground", pathname.startsWith(item.href) && "bg-white/10 text-foreground")}>{item.label}</Link>)}</nav><div className="flex items-center gap-2"><Button variant="outline" size="sm" asChild className="hidden sm:inline-flex"><Link href="/cards"><Search className="size-4" /> Search</Link></Button><Button size="sm" asChild><Link href="/admin"><Shield className="size-4" /> Submit</Link></Button><Button variant="ghost" size="icon" className="lg:hidden" aria-label="Open menu"><Menu className="size-5" /></Button></div></div></header>; }
