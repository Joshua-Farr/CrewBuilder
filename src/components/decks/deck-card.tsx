import { ArrowUpRight, Crown, Heart, Swords } from "lucide-react";
import Link from "next/link";
import { Badge } from "@/components/ui/badge";
import { Button } from "@/components/ui/button";
import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card";
import type { Deck } from "@/lib/types";
import { formatCurrency, getWinRate } from "@/lib/utils";
export function DeckCard({ deck }: { deck: Deck }) { return <Card className="group overflow-hidden transition hover:-translate-y-1 hover:border-sky-300/50"><div className="h-1 bg-gradient-to-r from-sky-400 via-violet-500 to-yellow-300" /><CardHeader><div className="flex items-start justify-between gap-4"><div><CardTitle className="text-lg">{deck.name}</CardTitle><p className="mt-1 text-sm text-muted-foreground">{deck.player} - {deck.tournamentName}</p></div><Badge variant={deck.placement === 1 ? "accent" : "default"}>#{deck.placement}</Badge></div></CardHeader><CardContent className="space-y-5"><div className="flex flex-wrap gap-2"><Badge variant="secondary"><Crown className="mr-1 size-3" />{deck.leaderName}</Badge>{deck.colors.map((color) => <Badge key={color} variant="outline">{color}</Badge>)}<Badge variant="outline">{deck.opSet}</Badge></div><div className="grid grid-cols-3 gap-3 text-center"><div className="rounded-2xl bg-white/5 p-3"><p className="text-lg font-black">{getWinRate(deck.wins, deck.losses, deck.draws).toFixed(1)}%</p><p className="text-xs text-muted-foreground">Win rate</p></div><div className="rounded-2xl bg-white/5 p-3"><p className="text-lg font-black">{deck.wins}-{deck.losses}</p><p className="text-xs text-muted-foreground">Record</p></div><div className="rounded-2xl bg-white/5 p-3"><p className="text-lg font-black">{formatCurrency(deck.estimatedCost)}</p><p className="text-xs text-muted-foreground">Cost</p></div></div><div className="flex items-center justify-between gap-3"><Button variant="ghost" size="sm"><Heart className="size-4" /> Save</Button><Button size="sm" asChild><Link href={`/decks/${deck.id}`}>Analyze <ArrowUpRight className="size-4" /></Link></Button></div><div className="flex items-center gap-2 text-xs text-muted-foreground"><Swords className="size-3" /> Key matchup: {deck.matchups[0]?.opponentLeaderName ?? "Open field"}</div></CardContent></Card>; }
