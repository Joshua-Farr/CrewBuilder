import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card";
import { Table, TableBody, TableCell, TableHead, TableHeader, TableRow } from "@/components/ui/table";
import { createMetadata } from "@/lib/seo";
import { getPlayers } from "@/lib/services/firestore";
export const metadata = createMetadata({ title: "Player Rankings", description: "Elo rankings and player performance for One Piece TCG events.", path: "/players" });
export default async function PlayersPage() { const players = await getPlayers(); return <div className="space-y-6"><h1 className="text-4xl font-black sm:text-6xl">Player Elo rankings</h1><Card><CardHeader><CardTitle>Competitive players</CardTitle></CardHeader><CardContent><Table><TableHeader><TableRow><TableHead>Player</TableHead><TableHead>Region</TableHead><TableHead>Elo</TableHead><TableHead>Record</TableHead><TableHead>Top cuts</TableHead></TableRow></TableHeader><TableBody>{players.map((player) => <TableRow key={player.id}><TableCell className="font-bold">{player.name}</TableCell><TableCell>{player.region}</TableCell><TableCell>{player.elo}</TableCell><TableCell>{player.wins}-{player.losses}</TableCell><TableCell>{player.topCuts}</TableCell></TableRow>)}</TableBody></Table></CardContent></Card></div>; }
